## To run project
```bash
pnpm install
pnpm run dev
```

## To build project
```bash
pnpm run build
```

**Component-Based Architecture**: Breaking UI into small, reusable components

**Centralized Theme Management**: Maintaining consistent styling through a theme system using tailwind centralized theme settings and also used sinigle theme object  for common resuable style across projects

**Responsive Design**: Ensuring the interface works well on all devices using tailwind resposive utilities

**Clean Code Practices**: Following consistent patterns across code

**User Experience Focus**: Adding loading states and clear visual feedback to user
